# Your machine's registry + local config — ship supa.registry.example, keep yours private
supa.registry
supa.config

# Compiled binaries (build with `deno task build`)
dist/

# config.toml backups written by `supa ports` / `supa rotate`
*.bak

# Per-project signing keys are private — never commit
**/signing_keys.json

# OS noise
.DS_Store
