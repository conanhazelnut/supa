{
  "version": "0.1.0",
  "license": "Apache-2.0",
  "tasks": {
    "dev": "deno run --allow-read --allow-write --allow-env --allow-run supa.ts",
    "check": "deno check *.ts",
    "lint": "deno lint *.ts",
    "fmt": "deno fmt *.ts",
    "test": "deno test -A",
    "ok": "deno fmt --check *.ts && deno lint *.ts && deno check *.ts && deno test -A",
    "build": "./build.sh",
    "build:win": "./build.sh windows"
  },
  "fmt": {
    "lineWidth": 100,
    "semiColons": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
