{
  "version": "0.1.0",
  "license": "Apache-2.0",
  "tasks": {
    "dev": "deno run --allow-read --allow-write --allow-env --allow-run supa.ts",
    "check": "deno check supa.ts",
    "lint": "deno lint supa.ts",
    "fmt": "deno fmt supa.ts",
    "build": "./build.sh",
    "build:win": "./build.sh windows"
  },
  "fmt": {
    "lineWidth": 100,
    "semiColons": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
